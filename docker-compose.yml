services:
  inspectarr:
    build: .
    container_name: inspectarr
    restart: unless-stopped
    ports:
      - "8585:8585"
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
